---
summary: "CLI reference for `marv uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `marv uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
marv uninstall
marv uninstall --all --yes
marv uninstall --dry-run
```
