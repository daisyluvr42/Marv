# src/ 目录重组方案

> 当前 `src/` 包含 **51 个子目录 + 17 个散文件**，共 **~3276 个文件**，全部平铺在一级。本文档梳理现状、映射到领域分组、并评估重组的风险和收益。

## 一、现状地图

### 当前 src/ 顶级目录完整清单

| 目录 | 文件数 | 当前职责 |
|------|--------|----------|
| `agents/` | 586 | Agent 运行时：PiEmbeddedRunner、tool 定义、system prompt、sandbox、skills、model 选择、subagent |
| `gateway/` | 250 | WebSocket/HTTP server、认证、路由、session 管理、MCP memory endpoint、hooks mapping |
| `commands/` | 281 | CLI 子命令实现：onboard、doctor、status、channels、models 等 |
| `cli/` | 219 | CLI 基础设施：program.ts、各种 -cli.ts 模块、browser-cli、daemon-cli |
| `config/` | 159 | 配置加载/验证/schema/类型、session 持久化、env substitution、legacy migration |
| `infra/` | 246 | 基础设施大杂烩：heartbeat、update、bonjour、ports、restart、outbound、push-apns、tailscale |
| `channels/` | 103 | Channel 抽象层：dock、registry、plugins、allowlist |
| `browser/` | 105 | Browser 自动化：Playwright、CDP、Chrome、extension relay |
| `telegram/` | 85 | Telegram adapter |
| `discord/` | 84 | Discord adapter |
| `memory/` | 81 | 记忆系统：embedding、hybrid search、QMD、soul memory store |
| `slack/` | 80 | Slack adapter |
| `cron/` | 64 | 定时任务引擎 |
| `line/` | 46 | LINE adapter |
| `hooks/` | 40 | Hook 系统：安装、加载、Gmail 集成 |
| `tui/` | 38 | 终端 UI |
| `daemon/` | 32 | 守护进程：launchd、systemd、schtasks |
| `signal/` | 31 | Signal adapter |
| `media/` | 27 | 媒体处理：image-ops、fetch、mime、store |
| `web/` | 78 | Web 渠道实现 |
| `shared/` | 25 | 共享工具类 |
| `process/` | 24 | 进程管理：exec、kill-tree、supervisor |
| `security/` | 23 | 安全审计：audit、dm-policy、fix |
| `auto-reply/` | 224 | 自动回复引擎 |
| `imessage/` | 22 | iMessage adapter |
| `logging/` | 20 | 日志系统 |
| `plugins/` | 55 | 插件系统 |
| `plugin-sdk/` | 18 | 插件 SDK |
| `terminal/` | 16 | 终端工具 |
| `markdown/` | 14 | Markdown 处理 |
| `media-understanding/` | 43 | 媒体理解(AI) |
| `link-understanding/` | 7 | 链接理解 |
| `acp/` | 12 | Agent Communication Protocol |
| `providers/` | 9 | Provider 特定逻辑(GitHub Copilot, Google, Qwen) |
| `routing/` | 6 | 消息路由 |
| `pairing/` | 7 | 设备配对 |
| `wizard/` | 13 | 安装向导 |
| `canvas-host/` | 6 | Canvas 宿主 |
| `node-host/` | 6 | Node 宿主 |
| `sessions/` | 8 | Session 策略补充 |
| `types/` | 9 | 第三方类型声明 (.d.ts) |
| `compat/` | 1 | 兼容层 |
| `ledger/` | 2 | 账本 |
| `macos/` | 4 | macOS 特定 |
| `docs/` | 1 | 内嵌文档 |
| `test-helpers/` | 2 | 测试工具 |
| `test-utils/` | 11 | 测试工具 |
| `tts/` | 4 | TTS |
| `utils/` | 27 | 通用工具 |
| `scripts/` | 1 | 脚本 |

### 散文件（应归入模块）

| 文件 | 建议归属 |
|------|----------|
| `index.ts` | 保留（入口） |
| `entry.ts` | 保留（入口） |
| `channel-web.ts` | → `channels/web/` |
| `globals.ts` | → `core/globals.ts` 或 `shared/` |
| `runtime.ts` | → `core/` |
| `extensionAPI.ts` | → `plugins/` |
| `polls.ts` / `polls.test.ts` | → `channels/plugins/` 或 `shared/` |
| `logger.ts` / `logger.test.ts` | → `logging/` |
| `logging.ts` | → `logging/` |
| `utils.ts` / `utils.test.ts` | → `utils/` |
| `version.ts` / `version.test.ts` | → `infra/` |
| `docker-setup.test.ts` / `dockerfile.test.ts` | → `infra/` 或 `test/` |

---

## 二、建议的领域分组方案

```
src/
├── index.ts                    # 保留
├── entry.ts                    # 保留
│
├── core/
│   ├── gateway/                # ← gateway/
│   ├── session/                # ← sessions/ + gateway/session-*.ts + config/sessions/
│   ├── config/                 # ← config/ (不含 sessions)
│   ├── routing/                # ← routing/
│   ├── security/               # ← security/
│   └── shared/                 # ← shared/ + globals.ts + runtime.ts
│
├── agent/
│   ├── runner/                 # ← agents/pi-embedded-runner* + pi-embedded-subscribe*
│   ├── tools/                  # ← agents/pi-tools* + bash-tools* + marv-tools* + channel-tools*
│   ├── prompt/                 # ← agents/system-prompt* + identity* + skills*
│   ├── model/                  # ← agents/model-* + models-config* + providers/
│   ├── sandbox/                # ← agents/sandbox* 
│   ├── subagent/               # ← agents/subagent-*
│   └── workspace/              # ← agents/workspace*
│
├── memory/
│   ├── search/                 # ← memory/hybrid* + search-manager* + mmr* + query-*
│   ├── storage/                # ← memory/sqlite* + soul-memory-store* + session-files*
│   ├── embeddings/             # ← memory/embeddings-* + batch-* + embedding-*
│   ├── sync/                   # ← memory/manager* + sync-*
│   └── api/                    # ← gateway/mcp-memory-http* + tools-invoke-http*
│
├── channels/
│   ├── _interface.ts           # ← channels/dock.ts + registry.ts
│   ├── common/                 # ← channels/ (allowlist, config, plugins, etc.)
│   ├── discord/                # ← discord/
│   ├── telegram/               # ← telegram/
│   ├── slack/                  # ← slack/
│   ├── signal/                 # ← signal/
│   ├── whatsapp/               # ← web/ (不是当前的 whatsapp/ — 见下文)
│   ├── imessage/               # ← imessage/
│   ├── line/                   # ← line/
│   └── web/                    # ← channel-web.ts + channels/web/
│
├── cli/
│   ├── program.ts              # ← cli/program.ts
│   ├── commands/               # ← commands/ + cli/*-cli.ts
│   └── tui/                    # ← tui/
│
├── media/                      # ← media/ + media-understanding/ + link-understanding/
│
├── infra/
│   ├── daemon/                 # ← daemon/
│   ├── process/                # ← process/
│   ├── cron/                   # ← cron/
│   ├── hooks/                  # ← hooks/
│   ├── update/                 # ← infra/update-*
│   ├── heartbeat/              # ← infra/heartbeat-*
│   └── common/                 # ← infra/ 剩余 (ports, dotenv, env, home-dir, etc.)
│
├── browser/                    # ← browser/ (保持独立)
│
├── plugins/                    # ← plugins/ + plugin-sdk/ + extensionAPI.ts
│
├── auto-reply/                 # ← auto-reply/ (保持独立)
│
└── _support/
    ├── types/                  # ← types/ (.d.ts)
    ├── logging/                # ← logging/ + logger.ts + logging.ts
    ├── utils/                  # ← utils/ + utils.ts
    ├── compat/                 # ← compat/
    └── test-utils/             # ← test-helpers/ + test-utils/
```

---

## 三、现状 vs 目标对照表

```mermaid
graph LR
    subgraph 现在_平铺
        A[agents/ 586]
        B[gateway/ 250]
        C[commands/ 281]
        D[config/ 159]
        E[infra/ 246]
        F["discord/ telegram/ slack/ ..."]
        G[memory/ 81]
    end
    
    subgraph 目标_领域
        A1[agent/runner]
        A2[agent/tools]
        A3[agent/prompt]
        A4[agent/model]
        B1[core/gateway]
        B2[core/session]
        B3[core/config]
        C1[cli/commands]
        E1[infra/daemon]
        E2[infra/process]
        E3[infra/heartbeat]
        F1[channels/discord]
        F2[channels/telegram]
        G1[memory/search]
        G2[memory/storage]
        G3[memory/embeddings]
    end
    
    A --> A1 & A2 & A3 & A4
    B --> B1 & B2
    C --> C1
    D --> B3
    E --> E1 & E2 & E3
    F --> F1 & F2
    G --> G1 & G2 & G3
```

---

## 四、关键原则与依赖方向

```
                  ┌────────────┐
                  │ _support/  │ ← 零依赖基础层
                  │ (logging,  │
                  │  utils,    │
                  │  types)    │
                  └─────┬──────┘
                        │
              ┌─────────┼─────────┐
              │         │         │
        ┌─────▼──┐  ┌───▼───┐  ┌─▼──────┐
        │  core/ │  │memory/│  │ media/ │
        │(config,│  │       │  │        │
        │gateway,│  │       │  │        │
        │session)│  │       │  │        │
        └──┬─────┘  └───┬───┘  └───┬────┘
           │            │          │
     ┌─────▼────────────▼──────────▼──────┐
     │              agent/                │
     │  (runner, tools, prompt, model)    │
     └─────────────────┬──────────────────┘
                       │
        ┌──────────────┼──────────────┐
        │              │              │
  ┌─────▼─────┐  ┌────▼────┐  ┌─────▼─────┐
  │ channels/ │  │   cli/  │  │  infra/   │
  │ (各渠道)   │  │         │  │           │
  └───────────┘  └─────────┘  └───────────┘
```

**核心规则**：
- `channels/telegram/` 只依赖 `channels/_interface.ts` + 第三方库 (grammy)，**不直接 import agent/**
- `memory/` 只依赖 `_support/` 和 `core/config/`，**不知道 channel 的存在**
- `agent/` 可依赖 `core/` 和 `memory/`，是业务逻辑的汇合点
- `cli/` 和 `infra/` 是顶层，可以依赖任何内层

---

## 五、风险评估与建议

> [!CAUTION]
> 此重组涉及 **~3276 个文件**，几乎所有 import 路径都需要更新。这是一个 **超高风险** 的重构操作。

### 风险

| 风险项 | 级别 | 说明 |
|--------|------|------|
| Import 路径批量修改 | 🔴 极高 | 所有相对路径 import 需要更新，一处遗漏即编译失败 |
| 测试基底崩塌 | 🔴 极高 | 大量 test-harness 和 mock 引用相对路径 |
| 分支冲突 | 🟠 高 | 如果有多人并行开发，merge 将几乎不可能 |
| tsconfig paths 调整 | 🟡 中 | barrel export 和 path alias 都需同步更新 |
| CI/CD 管道 | 🟡 中 | build 脚本、Dockerfile 中的路径引用 |

### 建议策略

> [!IMPORTANT]
> **不建议一次性全量移动**。建议采用**渐进式**策略：

1. **Phase 0 ：先清理散文件** — 把 17 个顶级散文件归入现有目录（低风险，高回报）
2. **Phase 1 ：Channels 提取** — 把 `discord/`, `telegram/`, `slack/`, `signal/`, `imessage/`, `line/`, `web/` 移入 `channels/` 下的子目录（这些模块相对独立，耦合低）
3. **Phase 2 ：Memory 细分** — 把 `memory/` 内部按 search/storage/embeddings 建子目录
4. **Phase 3 ：Agents 拆分** — 最大最危险的一步，把 586 个文件的 `agents/` 拆为 runner/tools/prompt/model/sandbox
5. **Phase 4 ：Gateway + Config → Core** — 把 gateway 和 config 包入 core/
6. **Phase 5 ：Infra 细分** — 把 246 个文件的 infra/ 按 daemon/heartbeat/update 拆分

每个 Phase 结束后：
- `tsc --noEmit` 确保编译通过
- `vitest run` 确保测试全绿
- 提交一个干净的 commit

---

## 六、是否现在执行？

这份文档是**分析和规划**，目的是帮你理清 `src/` 的结构。

关于是否真的执行重组，需要你确认：
1. 当前是否有多人并行在 `src/` 上开发？（如是，必须在开发间隙执行）
2. 你倾向于哪种粒度——仅做概念上的分组文档（不动代码），还是真的要移动文件？
3. 如果要移动，从哪个 Phase 开始？

---

## Verification Plan

如需执行重组，每个阶段验证如下：

### Automated Tests
```bash
# 每阶段完成后执行
npx tsc --noEmit           # TypeScript 编译检查
npx vitest run             # 全量单元测试
npx vitest run --config vitest.e2e.config.ts  # E2E 测试
```

### Manual Verification
- 检查 `import` 路径无残留旧路径：`grep -r "from ['\"]\.\./" src/ | head -50`
- 确认 build 产物正常：`npm run build`

---

## Phase 2 Walkthrough（2026-03-02）

本阶段已按计划完成 `memory/` 的内部细分，目标是把平铺文件按语义归类到 `search / storage / embeddings`，同时保持对外行为不变。

### 1) 目录重组结果

已在 `src/memory/` 下新增并使用：

- `src/memory/embeddings/`
- `src/memory/search/`
- `src/memory/storage/`

迁移后保留在 `src/memory/` 根目录的内容主要是对外入口和跨域核心：

- `index.ts`, `manager.ts`, `types.ts`, `internal.ts`, `backend-config.ts`, `status-format.ts`
- 以及 manager 相关测试与辅助测试文件

### 2) 文件迁移策略

- Embeddings 相关文件迁移到 `embeddings/`：
  - `batch-*`, `embedding-*`, `embeddings-*`, `node-llama.ts`, `openai-batch.ts`, `provider-key.ts`
  - `manager-embedding-ops.ts` 也一起迁移
- Search 相关文件迁移到 `search/`：
  - `hybrid.ts`, `mmr.ts`, `query-expansion.ts`, `search-manager.ts`
  - `qmd-*` 系列和 `temporal-decay.ts`
  - `manager-search.ts`
- Storage 相关文件迁移到 `storage/`：
  - `sqlite*.ts`, `session-files.ts`, `memory-schema.ts`, `soul-memory-*`
  - `sync-*` 系列和 `manager-sync-ops.ts`

### 3) Import 修复方法

- 使用“旧路径 -> 新路径”的 rename 映射进行批量改写，统一修复：
  - `src/memory` 内部跨模块引用
  - 外部模块对 memory 子模块的直接引用（如 `memory-tool`, `gateway`, `ledger`, `hooks`）
- 核心入口手动确认并更新：
  - `src/memory/index.ts` 改为导出 `./search/search-manager.js`
  - `src/memory/manager.ts` 改为引用 `./embeddings/*`、`./search/*`
  - `src/memory/manager-cache-key.ts` 改为引用 `./embeddings/headers-fingerprint.js`

### 4) 验证结果

执行结果如下：

- `npx tsc --noEmit`
  - 与本次 memory 迁移相关的报错已清零
  - 当前剩余报错为仓库既有的非 memory 问题（`daemon/*`, `gateway/*`, `infra/*` 等）
- `pnpm test -- src/memory`
  - 24 个 memory 测试文件全部通过（197 tests passed）
- `pnpm build`
  - 仍被既有问题阻断：`Cannot resolve entry module src/extensionAPI.ts`
  - 该失败在本次 phase 之前已存在，非 memory 重组引入

### 5) 本阶段结论

Phase 2（Memory 细分）已完成：目录结构完成重组、引用已修复、memory 相关测试全绿，可在此基础上继续执行 Phase 3（Agents 拆分）。

---

## Phase 3 Walkthrough（2026-03-02）

本阶段继续执行 `agents/` 拆分，目标是把原本大量平铺在 `src/agents/` 根目录的文件，按职责下沉到 `runner / tools / prompt / model / sandbox`。

### 1) 目录拆分结果

新增并使用：

- `src/agents/model/`
- `src/agents/prompt/`
- `src/agents/runner/`

同时补齐并扩展既有目录承载：

- `src/agents/tools/`（吸收 root 下 `pi-tools* / marv-tools* / bash-tools* / tool-*`）
- `src/agents/sandbox/`（吸收 root 下 `sandbox*.ts` 族）

### 2) 迁移范围（按族）

- Model 族（`model-*`, `models-*`, `*models*`, `synthetic/together/venice/minimax/pi-model-discovery`）整体迁移到 `agents/model/`
- Prompt 族（`system-prompt*`, `identity*`, `sanitize-for-prompt*`）迁移到 `agents/prompt/`
- Runner 族（`pi-embedded*`, `pi-auth-json*`, `pi-settings*`, `cli-runner*`, `claude-cli-runner*`）迁移到 `agents/runner/`
- Tools 族（`pi-tools*`, `marv-tools*`, `bash-tools*`, `bash-process-registry*`, `channel-tools*`, `tool-*`）迁移到 `agents/tools/`
- Sandbox 根文件（`sandbox*.ts`）迁移到 `agents/sandbox/`

### 3) Import 修复策略

- 使用“rename 映射驱动”的批量重写脚本修复相对路径
- 对两次迁移叠加导致的“相对路径二次偏移”问题，采用以下修复流程：
  1. 先将所有 moved 文件恢复为 `HEAD` 的旧内容
  2. 再基于完整 rename 映射执行一次性重写
- 额外修复 `src/plugins/extensionAPI.ts` 中对 `identity/model-selection` 的新路径引用

### 4) 验证结果

- `npx tsc --noEmit`
  - 与本次 Agents 拆分相关的新增错误已清零
  - 当前仅剩仓库既有基线错误（`daemon/*`, `gateway/*`, `infra/*` 等）
- `pnpm test -- src/agents/model src/agents/prompt src/agents/sandbox src/agents/runner src/agents/tools`
  - 45 个测试文件通过（273 tests passed）
- `pnpm build`
  - 仍被既有问题阻断：`Cannot resolve entry module src/extensionAPI.ts`（与本阶段拆分无关）

### 5) 本阶段结论

Phase 3 已完成核心拆分：`agents/` 的主干职责已下沉到 `runner / tools / prompt / model / sandbox`，并完成跨模块引用修复与回归验证，可继续进入 Phase 4（Gateway + Config -> Core）。

---

## Phase 4 Walkthrough（2026-03-02）

本阶段执行 `Gateway + Config -> Core`，把 `src/gateway` 与 `src/config` 收拢到 `src/core/` 下，作为后续 `core/session/shared` 进一步整理的基础。

### 1) 目录迁移结果

已完成整体迁移：

- `src/config/` -> `src/core/config/`
- `src/gateway/` -> `src/core/gateway/`

目录层级保留原结构（`protocol/`, `server/`, `server-methods/`, `sessions/` 等），仅调整上层归属。

### 2) Import 修复策略

- 先执行 `git mv`，保留 rename 历史
- 基于 rename 映射自动重写全仓相对导入路径（覆盖 `src/`, `ui/`, `extensions/`, `packages/`, `test/` 内 TS 文件）
- 重写后人工复跑类型检查，确认没有新增“找不到模块”问题

本阶段批量修正了 1000+ 文件的导入路径（一次性重写 1157 个文件）。

### 3) 验证结果

- `npx tsc --noEmit`
  - 与本阶段迁移相关的路径错误已清零
  - 当前剩余报错属于仓库既有基线问题（`daemon/*`, `infra/*`, `core/gateway/hooks.ts` 等）
- `pnpm test -- src/core/config src/core/gateway`
  - 102 个测试文件通过（895 tests passed）
- `pnpm build`
  - 仍被既有问题阻断：`Cannot resolve entry module src/extensionAPI.ts`

### 4) 本阶段结论

Phase 4 已完成核心目标：`gateway` 与 `config` 已进入 `core/` 命名空间，且已通过针对性测试验证，可继续执行 Phase 5（Infra 细分）。

---

## Phase 5 Walkthrough（2026-03-02）

本阶段执行 `Infra 细分`，把与基础设施生命周期最相关的模块按 `daemon / heartbeat / update` 收拢到 `src/infra/` 下的子目录，目标是降低 `src/` 顶层分散度并统一 infra 命名空间。

### 1) 目录迁移结果

已完成以下迁移：

- `src/daemon/` -> `src/infra/daemon/`
- `src/infra/heartbeat-*.ts`（及相关 test/harness/utils）-> `src/infra/heartbeat/`
- `src/infra/update-*.ts`（及相关 tests）-> `src/infra/update/`

迁移后，`daemon + heartbeat + update` 三组能力均位于 `src/infra/*` 下，目录语义与职责边界更清晰。

### 2) Import 修复策略

- 先使用 `git mv` 保留 rename 历史
- 再基于当前工作区 rename 映射执行批量相对导入重写（覆盖 `src/`, `ui/`, `test/`, `scripts/`, `extensions/`, `packages/`）
- 批量重写结果：
  - 第一次（heartbeat/update）：`Adjusted imports in 59 files`
  - 第二次（补齐 daemon 后）：`Adjusted imports in 203 files (585 import rewrites)`
- 额外手动确认了 `src/infra/heartbeat/*` 中 extension 测试导入路径，保持为 `../../../extensions/*`（从新目录层级计算后的正确路径）

### 3) 验证结果

- `npx tsc --noEmit`
  - 与本阶段迁移相关的路径错误已清零
  - 当前剩余报错仍为仓库既有基线问题：
    - `src/core/gateway/hooks.ts` (`trim` on `never`)
    - `src/core/gateway/server.chat.gateway-server-chat-b.e2e.test.ts` 签名不匹配
- `pnpm test -- src/infra/daemon src/infra/update`
  - 11 个测试文件通过（128 tests passed）
- `pnpm test -- src/infra/heartbeat`
  - 6 个 heartbeat 测试文件通过；5 个 suite 失败
  - 失败原因是既有基线问题：extension channel 文件导入 `marv/plugin-sdk` 解析失败（同样可在 `src/infra/outbound/outbound.test.ts` 复现），非本次路径迁移新增
- `pnpm build`
  - 仍被既有问题阻断：`Cannot resolve entry module src/extensionAPI.ts`

### 4) 本阶段结论

Phase 5 已完成核心目标：`daemon / heartbeat / update` 已统一进入 `src/infra/` 命名空间，并完成大范围导入修复。类型检查未引入新的迁移路径错误；测试层面仅剩仓库既有基线阻断，可在此基础上继续后续阶段或专项修复基线问题。

---

## Phase 6 Walkthrough（2026-03-02）

本阶段目标是先修复重构后继续推进所需的“基线阻塞项”，不做结构重排，优先恢复可构建与关键测试链路。

### 1) 修复项

- 修复 build 入口缺失：新增 `src/extensionAPI.ts` 兼容入口，转发到 `src/plugins/extensionAPI.ts`
- 修复 `hooks.ts` 类型错误：`x-marv-token` 头解析改为 `string | string[]` 安全分支，消除 `never.trim()`
- 修复 gateway e2e mock 签名：`server.chat.gateway-server-chat-b.e2e.test.ts` 中两处 `getReplyFromConfig` mock 补齐 `return undefined`，匹配返回类型
- 修复 Vitest 下插件 SDK 解析：
  - 将 `marv/plugin-sdk` alias 抽成共享配置
  - 下沉到每个 `defineProject(...)`（unit/extensions/gateway/live）
  - 保留顶层 alias 并补充 `server.deps.inline`，确保 `extensions/*` 导入链路统一走 Vite 解析

### 2) 关键验证

- `npx tsc --noEmit`：通过
- `pnpm build`：通过（此前 `src/extensionAPI.ts` unresolved entry 已修复）
- `pnpm test -- src/infra/heartbeat`：通过（11 files, 107 tests）
- `pnpm test -- src/infra/outbound/outbound.test.ts`：通过（61 tests）
- `pnpm test -- extensions/feishu/src/channel.test.ts`：通过（验证 `marv/plugin-sdk` 在 extensions 项目可解析）

### 3) 全量测试状态

- `pnpm test` 已可完整执行，但当前仍存在仓库既有失败（16 failed suites / 44 failed tests）
- 主要集中在既有模块回归与环境依赖问题（如 Matrix crypto 原生模块加载）
- 本阶段修复的阻塞项（build 入口、TS 基线错误、heartbeat + plugin-sdk 解析）已确认不再阻塞

### 4) 本阶段结论

Phase 6 已完成“基线解锁”目标：项目重新具备可构建能力，核心重构链路（infra heartbeat/update + extensions plugin-sdk 导入）恢复稳定，可继续进入下一阶段的功能/测试面收敛。

## Phase 7 Walkthrough（2026-03-02）

本阶段目标是完成全量测试收敛，解决 Phase 6 之后残留的并行测试不稳定问题，并恢复 `pnpm test` 全绿。

### 1) 问题定位

- 全量运行中剩余失败集中在：
  - `src/plugins/loader.test.ts`（11 failures）
  - `src/process/exec.test.ts`（偶发 1 failure）
- 复现后确认：
  - `loader.test` 在 `--pool=forks` 通过，在 `--pool=vmForks` 稳定失败。
  - 根因不是业务逻辑，而是测试编排：`test-parallel` 的 `unit-fast` 路径里 `--exclude` 在当前 Vitest 组合下未生效，导致本应 isolated 的文件被 `vmForks` 执行。

### 2) 改动内容

- `vitest.config.ts`
  - 新增 `unit-fast` project（`pool: "vmForks"`）。
  - 在该 project 内置 `unitFastExtraExclude`，把需要 isolated 的测试文件（含 `loader.test`、`exec.test`）从 fast lane 排除。
- `scripts/test-parallel.mjs`
  - `unit-fast` 运行项从 `--project unit` 改为 `--project unit-fast`。
  - 删除 `unit-fast` 上无效的 CLI `--exclude` 拼接，避免“看似排除、实际未排除”。
  - 将 `src/process/exec.test.ts` 加入 isolated 文件列表，降低高并发下的时序抖动。

### 3) 验证结果

- 针对性验证：
  - `pnpm vitest run --config vitest.config.ts --project unit-fast src/plugins`
    - `loader.test` 不再进入 `unit-fast`。
  - `pnpm vitest run --config vitest.config.ts --project unit --pool=forks src/plugins/loader.test.ts src/process/exec.test.ts`
    - 全通过（19 tests passed）。
- 全量验证：
  - `pnpm test`：通过（`731 passed / 731`, `6059 passed / 6059`）。
  - `pnpm build`：通过。

### 4) 本阶段结论

Phase 7 已完成：测试编排中的池化与隔离边界已修正，`pnpm test` 与 `pnpm build` 均恢复通过，可继续进入下一阶段功能性收敛或结构重组工作。
