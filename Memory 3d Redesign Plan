# 3D 记忆架构重构计划 (Importance, Clarity, Relevance)

您提出的这个【3D 记忆评估模型】非常深刻！它完全摒弃了传统的“查库即用”思维，转向了模拟人类大脑检索机能的**“动态唤醒模型”**。

这三个维度（重要性 Tier、清晰度 Confidence、相关性 Relevance）共同决定了最终**“这条记忆是否应该被强行塞进当前大模型的 Prompt 里”**（即所谓的注入阈值）。

以下是我根据 Marv/Marv 的本地管家特性，为您补齐的两个【待定算法】以及具体的实施计划。

---

## 核心算法补齐

### TBD 1.1: P2 自动晋升 P1 的判定机制 (The Experience Generalization)
**设计思路**：P1 代表能在多个场景复用的“通用工作经验”，是助理的办事实感。一条新产生的普通短效工作记忆（P2 半衰期 15天），不应该随意变成 P1。这必须是**跨越了单一项目限制，并在多个不同场景下都被验证为有效的规律**。

**P2 -> P1 晋升算法 (跨域涌现原则)**：
必须同时满足以下三个条件，在闲时被自动**静默晋升**为 P1（半衰期延长至 60天）：
1. **高清晰度门槛**：`Clarity >= 0.9` -> 意味着它已经被反复 Retrieve 并加固过多次。
3. **跨项目召回 (核心指标)**：它的调用记录里，包含了**至少 3 个不同的 Project Scope (项目上下文) 或 会话群组**。

**伴生技能提取 (Skill Extraction Bypass)**：
在 P2 晋升 P1 的瞬间，系统会自动触发一次 `Skill Extraction` (技能评估)。如果这条被广泛验证的经验包含清晰的“操作流”、“结构化判断”或“工具组合模式”，它不仅会成为 P1 层级的永久经验，而且会自动对接到原有的 Skill 自动提取流水线，直接注册并固化为一项可调用的系统 Skill。

### TBD 1.2: P1 晋升 P0 的严苛审判 (The Oracle Ascension)
**设计思路**：P0 是系统的最高级禁忌或全局真理，具有免疫遗忘的特权。**P2 绝对不可能跨级跳到 P0。** P0 的来源只有两个：
1. **用户显式设定**（初始系统设定、或者用户带强烈情绪强制强调的命令）。
2. **P1 级经验的史诗级固化**（极小概率事件）。

**P1 -> P0 晋升算法 (The 5-Epoch Trial)**：
一条 P1 的经验要固化为 P0，必须满足极其反人类的严苛条件，并经过用户的最后拍板：
1. **世纪大长考 (The 5-Epoch Survival)**：该条记忆的存活时间必须超过 **5个 P1 的半衰期**（即 `5 * 60 = 300 天`）。
2. **永不褪火 (Unfading Clarity)**：在这漫长的 300 天内，它的 `Clarity` 必须**始终维持在 0.8 以上**！这就意味着，在这近一年的时间里，用户在处理各种任务时（每隔十几天）都必须能自然召唤到这条记忆，维持着高频互动，一旦长时间冷落，Clarity 跌破 0.8，晋升计时器就会归零重置。
3. **神启批准 (The User Gate)**：当算法在夜间结算发现某条 P1 满足了上述条件，它**不会静默晋升**。它会在第二天用户上线时，弹出一个**系统级批准通知 (User Approval Notification)**。
   - *话术示例*：“过去三百天里，由于我们经历了大量的异常处理，我发现您极其注重『不要吞噬底层报错堆栈』这一原则。是否将其升级为您的核心系统性禁忌 (P0)？”
   - **如果用户点击同意**：正式升格为 P0。
   - **如果用户点击否定**：晋升被否决，继续作为 P1 存在，或者用户可以直接借机修剪它。

**P0 实体化映射 (The Soul Document)**：
P0 级记忆为了确保最高的透明度和控制权，具备一种实体形态——它会同步追加到一个对人类完全友好的独立 Markdown 文档（例如 `P0_SOUL_MEMORY.md`）。
*   **人类权限 (God Mode)**：完全的读写自由。用户可以直接打开这个 MD 文档随意涂改、删减。文档的变动会自动反向同步回系统数据库（即用户的初始设定来源）。
*   **Agent 权限 (Append Only)**：**极其严格的单向追加权**。Agent 只有在发起的 P1 -> P0 晋升获得用户点击同意时，才有权限往这个文档末尾写入一条新增内容。Agent **绝对没有权限**删除或篡改该文档里的任何历史文字。这保证了核心设定的绝对不可侵犯。

### TBD 2: 相关性 (Relevance) 匹配度算法
**设计思路**：这解决了“什么记忆能在当前任务里被唤醒”。如果在进行前端项目开发（项目池 A），后端的记忆（项目池 B）相关性应该被极具打压，而大盘通用记忆（P0 用户偏好）应该适度透出。在这个维度的计算上，我们应该充分利用早前确立的 **5-Signal 融合检索策略 (Nowledge Mem Architecture)**，让语义匹配更加立体。

**匹配度算法 (全息融合交叉惩罚)**：
`Relevance Score = (Fusion Semantic Match) * (Scope Penalty)`

1. **Fusion Semantic Match (五维融合语义匹配)**：0~1。直接复用 `soul-memory-store.ts` 中精心调配的融合打分引擎：
   - *Vector Score (向量余弦，权重 0.32)*
   - *Lexical Score (词法重叠，权重 0.15)*
   - *BM25 Score (FTS5全文倒排，权重 0.15)*
   - *Graph Score (实体关联图谱，权重 0.16)*
   - *Cluster Score (聚类亲和度，权重 0.06)*
   将以上五个维度的单项得分进行最终加权求和，得到一个极其精准的纯内容匹配度。
2. **Scope Penalty (上下文惩罚圈)**：
   提取当前正在运行的任务或对话的 `Current_Scope` (例如 `project:marv_v2`)。
   - **核心匹配圈 (权数 1.0)**：记忆本身的 `scope` = `Current_Scope`。原汁原味，无惩罚。
   - **大盘辐射圈 (权数 0.8)**：记忆本身的 `scope` = `global/user` (比如你的全局基础设定)。适度惩罚。
   - **跨界噪音圈 (权数 0.2)**：记忆属于其他平行的无关项目。遭到**极重惩罚**，除非这段记忆的融合匹配度高到爆炸，否则极难突围进入最终调用。

### TBD 3: 清除机制与 P0 特权唤醒 (The Clearance & Recall Mechanism)
**设计思路**：人脑的记忆容量是有限的，如果一条普通经验长期未被召回且自然衰退到了冰点，它应该被彻底抹除。但对于“核心禁忌 (P0)”，它可能长期不触发（因其平时 relevance 低），但一旦碰壁，必须能瞬间复活。

**清除算法 (Garbage Collection)**：
引入定期的“记忆修剪进程 (Pruning Task)”或在每次查询后顺手触发清理：
*   **对于 P1 和 P2 级记忆**：计算其当前的实际 Clarity（初始 Clarity * 衰减系数）。一旦 **`实际 Clarity < 0.1`**，系统静默将其从数据库中执行物理删除 (`DELETE FROM memory_items WHERE ...`)。真正的遗忘机制诞生。
*   **对于 P0 级记忆**：**永远不会被物理清除**（哪怕 Clarity 因久未命中而跌到谷底，它也只是在平时查询中因权重极低而蛰伏不动）。
*   **P0 特权跳变 (Instant Recall Boost)**：如果在某次搜索中，虽然某条 P0 记忆的真实 Clarity 极低，但它的**环境相关性 (`Relevance Score`) 突然极高**（即当前任务与该古老禁忌严丝合缝），此时系统会触发 **瞬时唤醒 (Instant Recall)**：直接无视衰减公式，将其 Clarity 满血复原至 `1.0` 并注入 Prompt。

---

## 系统级最终调用公式 (The Inject Function)

在最终决定是否将某条记忆注入到 Prompt 时，所有查询到的候选记忆将按以下步骤清洗与计算：

### 阶段一：遗忘与修剪倒计时 (The Decay & Pruning Phase)
*   `Age` = `Current_Time - Last_Accessed_At`
*   **P1**: `当前 Clarity = 初始 Clarity * exp(-λ * Age)` (半衰期 60 天)
*   **P2**: `当前 Clarity = 初始 Clarity * exp(-λ * Age)` (半衰期 15 天)
*   **物理清除判定**：如果在这一步算完，P1/P2 记忆的 `当前 Clarity < 0.1`，直接跳过并触发异步数据库**物理删除任务 (`DELETE`)**。
*   **P0 (神之意志)**: 它的 `当前 Clarity` 同样会因久不访问而衰减跌破 0.1（以降低无用查询权重），但它**免疫物理清除**，被允许蛰伏。

### 阶段二：打分与 P0 特权唤醒 (The Scoring & Recall Phase)
计算相关性得分 `Relevance` (Semantic Match * Scope Penalty)。
在此刻，对所有存活的候选者计算最终注入得分：
**`Inject Score = Importance_Tier_Multiplier * 当前 Clarity * Relevance`**

*   **1. Importance_Tier_Multiplier (阶层权重)**:
    *   P0 = 1.25x （自带高权系数）
    *   P1 = 1.0x
    *   P2 = 0.85x
*   **2. P0 瞬时特权跳变 (Recall Boost)**: 
    如果在计算完发现某条 P0 记忆此时的 `Relevance` 非常高（例如 `> 0.8`，代表当前任务正好命中了这条古老禁忌的要害），不管它此前的 `当前 Clarity` 跌得有多烂，系统**立刻强行将其 Clarity 覆盖为 `1.0`**（满血复活），从而算出突破天际的 `Inject Score`，霸榜并拉取进 Prompt 中。

### 阶段三：注入与重塑验证 (The Injection & Reinforce Phase)
最终，只有 **`Inject Score >= 设定阈值 (例如 0.65)`** 的记忆，才会被序列化进发给大模型的系统提示词中。
凡是被成功注入到本次上下文中的记忆（即“这回合我确实想起了你”），其 `Last_Accessed_At` **立刻更新为 Now**，阻止其继续衰变。同时对命中 P0 的条目，因为 Clarity 被真实提拉回 1.0 开始重新计算半衰，这就是“用进废退”闭环。

---

## 代码重构落地步骤 (针对现有的 `src/memory/soul-memory-store.ts`)

1. **修改时效常数**：将现有的半衰期常数修改为确切的：P1 = 60 (不仅限活跃衰减，把内容衰减也同步为 60)，P2 = 15，P0 = Infinity。
2. **修改 DB 表以支持跨域判定**：新增一张小表 `memory_scope_hits (memory_id, scope_id, hit_count)`。每次某条记忆在某特定 scope 中被成功注入，就记录一条 Hit。
3. **改造 `querySoulMemoryMulti`**：
   - 砍掉现有复杂冗余的魔法常数。
   - 彻底按照您的 3D 模型重组计算流：先算 Semantic Relevance，乘以 Scope Penalty，再乘以衰减后的 Clarity，最后乘阶层系数。
4. **新增夜间反射/检查机制 (`promoteMemories` Task)**：每次系统空闲或者内存整理时，扫一遍数据库：
   - **执行 P2 -> P1 静默晋升**：将 `hit_count >= 1` 的不重复 scope_id 数量加起来，如果是 P2 且数量 `>= 3` 且存活 `> 14 天`，触发 `UPDATE table SET tier = 'P1'`。
   - **执行 P1 -> P0 神启试炼**：扫描所有 P1 记忆，计算其存活时间。若存活时间 `>= 300 天` (5个半衰期) 且当前 `Clarity >= 0.8` (证明中途从未遇冷跌破阈值)，则收集这些条目，在次日生成系统通知，交由用户点击授权。一旦授权，执行 `UPDATE table SET tier = 'P0'`。
